<com.cassianetworks.mylibrary.views.DampScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dcv"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg"
    android:fadingEdge="none"
    android:fillViewport="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.cassianetworks.mylibrary.views.MyGridView
            android:id="@+id/gv_device"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:cacheColorHint="@android:color/transparent"
            android:fastScrollEnabled="false"
            android:gravity="center"
            android:listSelector="@android:color/transparent"
            android:numColumns="2"
            android:padding="20dp"
            android:scrollbars="none"
            android:verticalSpacing="10dp" />

        <ImageView
            android:id="@+id/iv_add"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginLeft="40dp"
            android:src="@mipmap/add" />
    </LinearLayout>
</com.cassianetworks.mylibrary.views.DampScrollView>